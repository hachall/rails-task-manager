<h1>Tasks</h1>
<% @tasks.each do |task| %>
<h2><%= task.name %></h2>
<p><%= task.details %></p>
<p><%= task.completed ? "Completed!" : "Not Completed!" %></p>
<%= link_to "Delete", task_path(task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
<% end %>
